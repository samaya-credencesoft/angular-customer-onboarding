import { InjectionToken } from '@angular/core';
import { OktaAuthService } from './okta.service';
import { OktaAuthGuard } from './okta.guard';
import { OktaConfig } from './okta.config';
export declare class OktaAuthModule {
    static initAuth(config: OktaConfig): {
        ngModule: typeof OktaAuthModule;
        providers: (typeof OktaAuthService | typeof OktaAuthGuard | {
            provide: InjectionToken<OktaConfig>;
            useValue: OktaConfig;
        })[];
    };
}
